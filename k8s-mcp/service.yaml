apiVersion: v1
kind: Service
metadata:
  name: ansible-mcp
  namespace: cattle-ai-agent-system
spec:
  selector:
    app: ansible-mcp
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
